services:
  - type: web
    name: portfolio
    runtime: node
    nodeVersion: 20.11.0
    buildCommand: npm ci && npm run build
    staticPublishPath: ./out
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
